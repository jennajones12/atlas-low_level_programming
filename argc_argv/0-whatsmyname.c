#include "main.h"

/**
 * main - prints its name, followed by new line
 * @argc: argument count
 * @argv: argument value
 * Return: always 0
 */
int main (int argc, char *argv[])
{
	printf("Program name: %s\n", argv[0]);
	return (0);
}
