#include "main.h"
/**
 * more_numbers - prints 10 times the numbers, from 0 to 14, a new line
 *
 * Return: null
 */

void more_numbers(void) {
    for (int i = 0; i < 15; i++) {
        printf("%d\n", i * 10);
    }
}
