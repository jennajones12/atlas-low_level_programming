#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @i: input integer
 * Return: positive 1, negtive -1 and 0 0
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
